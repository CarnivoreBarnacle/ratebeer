
<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>

<p>
  <strong>Style:</strong>
  <%= @beer.style.name %>
</p>

<p>
  <strong>Brewery:</strong>
  <%= @beer.brewery_id %>
</p>

<p>
	<% if @beer.ratings.empty? %>
 		Beer has not yet been rated!
	<% else %>
 		Has <%= pluralize(@beer.ratings.count, 'rating', 'ratings')%>,
 		Average score: <%= @beer.average_rating%>
	<% end %>
</p>


<% if current_user %>
  <br>
  <h4>give a rating:</h4>

  <%= form_for(@rating) do |f| %>
    <%= f.hidden_field :beer_id %>
    score: <%= f.number_field :score %>
    <%= f.submit %>
  <% end %>

<% end %>
<%= link_to 'Edit', edit_beer_path(@beer) %> | <%= link_to 'Back', beers_path %>
